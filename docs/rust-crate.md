# Rust crate

**Coming soon.** This entry is a reserved placeholder: `processkit-py` is the
Python binding of the [`processkit`](https://crates.io/crates/processkit) Rust
crate, which already exists on crates.io. A dedicated crate-facing
documentation link for it will be filled in here once it is ready — this page
is not a substitute for it yet.
